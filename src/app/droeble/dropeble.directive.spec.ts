import { DropebleDirective } from './dropeble.directive';

describe('DropebleDirective', () => {
  it('should create an instance', () => {
    const directive = new DropebleDirective();
    expect(directive).toBeTruthy();
  });
});
