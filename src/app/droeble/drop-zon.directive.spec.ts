import { DropZonDirective } from './drop-zon.directive';

describe('DropZonDirective', () => {
  it('should create an instance', () => {
    const directive = new DropZonDirective();
    expect(directive).toBeTruthy();
  });
});
