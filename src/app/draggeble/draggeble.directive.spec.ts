import { DraggebleDirective } from './draggeble.directive';

describe('DraggebleDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggebleDirective();
    expect(directive).toBeTruthy();
  });
});
